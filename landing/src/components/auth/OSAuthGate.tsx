/**
 * OSAuthGate — DISABLED (Phase 36 · 02.03.2026)
 * Auth wall removed for hackathon judges / demo access.
 * Any legacy imports of this component will get a pure passthrough.
 */
export function OSAuthGate({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
